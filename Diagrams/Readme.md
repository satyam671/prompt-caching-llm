# Diagrams
This folder contains prompt architecture diagram that shows why structure determines cache hit rate, prompt caching cost impact (token cost per call and monthly cost at scale), prompt caching pricing comparision chart that shows regular input vs cache write vs cache read and invalidation visual showing what cache invalidation looks like in production, 
